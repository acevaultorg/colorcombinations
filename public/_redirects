# Cloudflare Pages _redirects file
# https://developers.cloudflare.com/pages/configuration/redirects/

# Trailing-slash normalization is handled by Astro (trailingSlash: "always")

# ---------------------------------------------------------------------------
# JSON API — accept extension-less index paths and rewrite to .json so
# /api/v1/palettes and /api/v1/palettes.json both work. Per-item URLs
# must use the .json extension explicitly (the :slug wildcard would also
# match `black.json` → `black.json.json` which doesn't exist).
# ---------------------------------------------------------------------------
/api/v1/palettes              /api/v1/palettes.json              200
/api/v1/palettes/             /api/v1/palettes.json              200
/api/v1/collections           /api/v1/collections.json           200
/api/v1/collections/          /api/v1/collections.json           200
/api/v1/colors                /api/v1/colors.json                200
/api/v1/colors/               /api/v1/colors.json                200

# V2: if sumitome.com is later purchased as an alias, add:
#   https://sumitome.com/*  https://colorcombinations.org/:splat  301
